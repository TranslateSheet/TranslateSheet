#!/usr/bin/env node
/**
 * Generate the primary language file.
 */
declare const generatePrimaryLanguageFile: (outputDir: string, translations: Record<string, any>) => void;
export default generatePrimaryLanguageFile;
